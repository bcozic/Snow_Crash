Here, an email informs us there is a cronjob executed every
2 minutes. This job do a bash -x with all files in /opt/openarenaserver.
Since we can write there, we create a flag file containing: "getflag > /tmp/flag"

2 minutes later, we have our flag.

We can now get the next flag.
